# 后续功能

以下仅记录**尚未完成**的增强。技术路线评估见 [TAURI2-MIGRATION-VALIDATION.md](TAURI2-MIGRATION-VALIDATION.md)。

## 已完成（勿重复排期）

| 项 | 说明 |
| --- | --- |
| 发布校验 + 默认仅 NSIS | `verify:release`；`build:win` 不打 ZIP |
| 打印入口策略 | 全部 / 仅当前 / 不自动加入，默认全部 |
| PDF 搜索 / 命中跳转 / 文字层复制 | 工具栏搜索 + 查找按钮 |
| PDF 目录书签 + 缩略图 | 最多 40 页；切换页签不空白 |
| 打印纸张排版示意 | 约 10mm 边距适配所选纸的缩略预览 |
| Excel 行虚拟滚动 | DOM 有界 + 上下占位 |
| 性能基准 | `npm run bench` / `npm run bench:ui`（suite bench） |
| 预览「+ 打开」 | 标签栏追加文件；Ctrl+O |
| 沉浸隐藏 PDF 工具条 | 全屏内容占满视口 |

## 剩余可优化（去重后）

### P2 — 打印与兼容

| ID | 内容 | 工作量 | 验收要点 |
| --- | --- | --- | --- |
| P2-6 | 打印缩放与页序：实际大小 / 适合纸张 / 仅缩小；当前页、奇偶页、逆序 | 中 | **已完成**（范围+页序） |
| P2-7 | 打印机能力检测 + 打开系统打印队列；UI 区分「已提交」vs「出纸完成」 | 中至大 | **已完成**（队列入口 + 状态文案；驱动能力为尽力展示） |
| P2-8 | PDF 密码输入；加密/损坏分类提示；阅读区临时旋转（不改源文件） | 中 | **已完成** |
| P2-9 | Word 表格临时列宽跨窗口迁移（Excel 已迁） | 中 | **已完成**（view.word.widths） |
| P2-10 | Office 复杂排版最小复现样例库（PPT/Word 表格、字体） | 持续 | **已起步**（complex-table.docx + OFFICE-SAMPLES.md；持续收录） |

### P3 — 文档侧

| ID | 内容 | 工作量 | 验收要点 |
| --- | --- | --- | --- |
| P3-11 | Word / 图片打印（PDF 打印稳定后再做） | 大 | 分页、表格、实际出纸 |
| P3-12a | Excel 冻结行列、单元格定位 | 中至大 | 宽表 + 合并区域 |
| P3-12b | 文本行号、搜索、代码高亮 | 中 | 大文件、编码切换 |
| P3-12c | 图片旋转 / 原始像素查看 | 中 | 缩放与原文件不变 |

### 工程 / 质量

| ID | 内容 | 说明 |
| --- | --- | --- |
| Q-1 | 安装包代码签名 | 未商业签名，SmartScreen 可能拦；需证书与流水线 |
| Q-2 | 安装版 Open With / 卸载手工验收 | 已做「卸载重装到 D:\file-preview」；资源管理器右键与干净卸载仍建议实机点一遍 |
| Q-3 | 实体打印机出纸验收 | 自动化只做内存 PDF；A4/A5、双面、份数需真机 |
| Q-4 | ResizeObserver 偶发警告 | 长 PDF 调窗口仍可能出现；未宣称已修 |
| Q-5 | 超多页 PDF DOM 虚拟化 | 仍每页占位；极大页数未做窗口化 |
| Q-6 | 打印多窗口时序测试加固 | printing 套件对窗口生命周期敏感 |
| Q-7 | SmartScreen 未签名引导文案 | README/安装说明可补 |

## 与用户旧清单的对照（已剔除重复）

| 旧条目 | 状态 |
| --- | --- |
| 打印排版预览 | **已有示意版**（P1）；完整「纸张上的可视化排版/逐页尺寸浏览」可并入 P2-6 或单列 |
| PDF 搜索/高亮/复制 | **已完成** |
| PDF 缩略图/目录 | **已完成** |
| Excel 虚拟滚动 | **已完成**（冻结行列仍是 P3-12a） |
| 长 PDF 基准 | **已有脚本**；真实大样本对比仍建议做（可并入 Q 或性能专项） |
| 打印入口 all/current/none | **已完成** |

## 打印后续（仅未做部分）

独立打印窗口、逐文件参数、并排、入口策略、排版示意已交付。未做：混合尺寸**逐页尺寸列表**、打印机能力自动检测、页序/适配模式（P2-6/7）。

## 标签移动状态

已迁：适配、Excel 工作表/列分组/已加载行、编码、Excel 列宽。**未迁：Word 临时列宽（P2-9）**。

## 技术路线：Electron → Tauri 2

打印等硬门槛未过前不替换主栈。见 [TAURI2-MIGRATION-VALIDATION.md](TAURI2-MIGRATION-VALIDATION.md)。
