# 后续功能

以下仅记录未完成的增强方向。技术路线评估见 [TAURI2-MIGRATION-VALIDATION.md](TAURI2-MIGRATION-VALIDATION.md)。

## 优先级与工作量（2026-09-14 修订）

先优化现有能力，再开发新功能。以下为工程范围估计，不是耗时承诺；每批开工前重新评估。

| 优先级 | 内容 | 工作量 | 验收 |
| --- | --- | --- | --- |
| P0 已完成 | 发布产物结构检查、ZIP CRC、SHA256 清单；默认仅 NSIS 安装包 | 小 | 完整产物通过，截断安装包失败 |
| P0 已完成 | 打印入口可配置（全部 / 仅当前 / 不自动加入，默认全部） | 小 | printing + core 套件通过 |
| P1 已完成 | 长 PDF/Excel 基准脚本：`npm run bench`（库级）与 `npm run bench:ui`（打开→ready） | 小 | 结果写入 outputs/bench/ |
| P1 已完成 | PDF 搜索/命中跳转；打开搜索后文字层可选中复制 | 中 | pdf 套件：Local Preview 命中与高亮 |
| P1 已完成 | PDF 目录/书签 + 缩略图侧栏（最多 30 页缩略） | 中 | pdf 套件导航与缩略图 |
| P1 已完成 | 打印纸张排版示意（约 10mm 边距适配所选纸张） | 中 | printing 套件 paper-layout-preview |
| P1 已完成 | Excel 行虚拟滚动（仅渲染可视窗口 + 上下占位） | 大 | excel 套件 DOM 有界与回顶 |
| P2 | 实际大小/适合纸张/仅缩小、当前页/奇偶页/逆序打印 | 中 | 页序、缩放比例与边界输入 |
| P2 | 打印机能力检测、打开系统打印队列 | 中至大 | 不同驱动实测；区分提交和出纸 |
| P2 | PDF 密码输入、分类错误提示、临时旋转 | 中 | 加密/损坏分别提示，密码不保存、旋转不改源文件 |
| P2 | Word 临时表格列宽跨窗口迁移 | 中 | 转移成功、失败回滚；Excel 迁移已完成 |
| P2 | Office 复杂排版兼容性样例库 | 持续 | 真实最小复现文件逐项回归 |
| P3 | Excel 冻结行列、单元格定位、搜索 | 中至大 | 宽表、合并区域、多 Sheet |
| P3 | 图片旋转/原始像素；文本行号/搜索/代码高亮 | 中 | 缩放、编码、大文本及原文件不变 |
| P3 | Word 与图片打印 | 大 | 排版保真、分页、实际打印；PDF 打印稳定后实施 |

## 工程与质量（当前缺口）

| 优先级 | 内容 | 说明 |
| --- | --- | --- |
| P1 | 真实大文件性能基准 | 现有 80 页合成样例不能代表百页混合/万行 Excel；需要前后耗时与内存对比脚本 |
| P1 | 本机安装包验收 | 安装、卸载、Open With、托盘、设置持久化未在本轮安装路径上实机跑完 |
| P2 | 安装包代码签名 | 未商业签名，SmartScreen 可能拦截；需要证书与流水线 |
| P2 | 实体打印机验收 | 自动化仅拦截 print 并做内存 PDF；驱动、纸盒、双面需实机 |
| P2 | ResizeObserver 偶发警告 | 长 PDF 调窗口仍可能触发；未宣称已修复，需复现与根治 |
| P2 | 超多页 PDF DOM | 每页仍建占位元素，极大页数未做虚拟列表 |
| P2 | 打印入口/多窗口测试稳定性 | 多开打印窗口时序依赖较强，改打印面板时回归成本高 |
| P3 | 打印未签名路径下的 SmartScreen 文案与用户引导 | README 可补充 |

本轮边界：先加交付校验与可配置打印入口；安装包校验不能替代安装测试、签名或内容安全审计。

## 打印后续增强

已实现独立打印窗口、每文件独立设置、预览联动、并排查看，以及打印入口策略（默认本窗口全部 PDF）。后续可增加混合尺寸文档逐页尺寸浏览、纸张上的可视化排版预览，以及打印机能力自动检测；目前不代表已支持这些功能。

## 标签移动状态的进一步保留

当前版本已有：跨窗口拖入合并、目标确认后移除源标签、同窗切换/排序/关闭、保留缩放与常规滚动位置。

后续可增强：

- 已实现迁移适配方式、Excel 工作表/列分组/已加载行、文本编码和临时列宽；Word 表格临时列宽迁移仍待开发。
- 空窗口、最后一个标签、关闭与拖动并发时有明确行为。
- 测试两窗互拖、排序、关闭、转移失败回滚、大文件内存释放及主题同步。

## 文档兼容性样例库

持续收集无隐私的最小复现文件，优先针对真实出现的 PPT、Word 复杂表格与特殊字体问题建立回归。每次修复仅声明经过验证的范围。

## 技术路线：Electron → Tauri 2

当前实现深度依赖 Electron（多窗口、打印 `webContents.print` / `printToPDF`、自定义 `preview://` 协议、托盘、单实例、Open With、内存 session）。**不建议在未完成校验清单前直接重写主进程。** 完整 PoC 门禁与回退条件见 [TAURI2-MIGRATION-VALIDATION.md](TAURI2-MIGRATION-VALIDATION.md)。
