# 后续功能

以下仅记录未完成的增强方向。

## 优先级与工作量（2026-09-13）

先优化现有能力，再开发新功能。以下为工程范围估计，不是耗时承诺或 GPT 额度预测；每批开工前重新评估与查询额度。

| 优先级 | 内容 | 工作量 | 验收 |
| --- | --- | --- | --- |
| P0 已完成 | 发布产物结构检查、ZIP CRC、SHA256 清单 | 小 | 完整产物通过，截断安装包失败；core 重试通过、D 盘安装版 package 通过 |
| P1 部分完成 | 长 PDF 首屏优先、后台分批读取尺寸已实现；真实大文件耗时基准待补 | 中 | 80 页混合尺寸、末页跳转、快速缩放；待前后耗时对比 |
| P1 | Excel 虚拟滚动与解析性能 | 大 | 万行样例内存/滚动对比，合并单元格、图片、列宽回归 |
| P1 | PDF 搜索、高亮、文本选择复制 | 中至大 | 中文、跨行、多页结果、扫描页明确限制 |
| P1 | PDF 缩略图、目录与书签导航 | 中 | 定位准确、长文档资源有界 |
| P1 | 打印纸张排版预览、逐页尺寸 | 大 | A4/A5 混合横竖页、边距与真实出纸验收 |
| P2 | 实际大小/适合纸张/仅缩小、当前页/奇偶页/逆序打印 | 中 | 页序、缩放比例与边界输入 |
| P2 | 打印机能力检测、打开系统打印队列 | 中至大 | 不同驱动实测；区分提交和出纸 |
| P2 | PDF 密码输入、分类错误提示、临时旋转 | 中 | 加密/损坏分别提示，密码不保存、旋转不改源文件 |
| P2 | 跨窗口保留工作表、编码、临时列宽 | 中 | 转移成功、失败回滚与并发关闭 |
| P2 | Office 复杂排版兼容性样例库 | 持续 | 真实最小复现文件逐项回归 |
| P3 | Excel 冻结行列、单元格定位、搜索 | 中至大 | 宽表、合并区域、多 Sheet |
| P3 | 图片旋转/原始像素；文本行号/搜索/代码高亮 | 中 | 缩放、编码、大文本及原文件不变 |
| P3 | Word 与图片打印 | 大 | 排版保真、分页、实际打印；PDF 打印稳定后实施 |

本轮边界：先加交付校验，不承诺已经提升预览速度。安装包校验不能替代安装测试、签名或内容安全审计。

## 打印后续增强

已实现独立打印窗口、每文件独立设置、预览联动和并排查看。后续可增加混合尺寸文档逐页尺寸浏览、纸张上的可视化排版预览，以及打印机能力自动检测；目前不代表已支持这些功能。

## 标签移动状态的进一步保留

当前版本已有：跨窗口拖入合并、目标确认后移除源标签、同窗切换/排序/关闭、保留缩放与常规滚动位置。

后续可增强：

- 已实现迁移适配方式、Excel 工作表/列分组/已加载行、文本编码和临时列宽；Word 表格临时列宽迁移仍待开发。
- 空窗口、最后一个标签、关闭与拖动并发时有明确行为。
- 测试两窗互拖、排序、关闭、转移失败回滚、大文件内存释放及主题同步。

## 文档兼容性样例库

持续收集无隐私的最小复现文件，优先针对真实出现的 PPT、Word 复杂表格与特殊字体问题建立回归。每次修复仅声明经过验证的范围。
