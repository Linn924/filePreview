# 窗口状态、错误提示与 Office 容错（2026-09-13）

跨窗口标签迁移携带适配模式、文本编码及 Excel 工作表、列分组、已加载行、临时列宽；仅保存在 PreviewFile 的内存 view 中，不写入应用设置或文件历史。Word 临时表格列宽尚未迁移。

PDF/Word/PPT/Excel 初始解析错误统一转换为可操作提示，区分可识别的加密、损坏、文件缺失与读取权限问题，不直接显示内部路径和解析器堆栈。未识别错误仍采用兼容性建议，不推断其确定原因。

XLSX 先读取基础单元格，再尝试样式引擎；后者失败时显示降级提示并保留基础预览。有效 XLSX 的样式解析失败通过故障注入测试，损坏 ZIP 仍失败。此容错不代表新增复杂图表、SmartArt 或旧 Office 格式保真能力。

验证：类型检查、生产构建、all 全量回归通过；源样例 SHA256 不变。补充 core/excel 定向验证适配、编码、工作表和临时列宽迁移。长 PDF 窗口调整仍偶发 ResizeObserver 通知警告，测试通过；保留此前记录，未将其宣称为已修复。实体打印与本轮新版安装包尚未验证，本轮交付为源码提交。
