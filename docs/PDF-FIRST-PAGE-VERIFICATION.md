# PDF 首屏优化（2026-09-13）

第一页尺寸读取与绘制先完成，随后通知界面可阅读，再逐批读取剩余页尺寸（每批最多 16 页）。初始占位使用第一页尺寸，实际页面渲染前校正尺寸；更新尺寸时尽量保持当前阅读位置，总页数从开始就完整显示。未改动打印实现。

验证：类型检查、生产构建以及 pdf,immersive,printing 套件通过。新增 80 页横竖交替样例，验证页数、各页比例、末页绘制和快速缩放；原文件 SHA256 不变。短页测试在大屏上没有滚动空间，现固定测试窗口 1000×700。一次窗口调整出现 ResizeObserver 通知警告，回归仍通过，后续若用户遇到闪动需继续调查。

限制：尚无真实大文件的前后耗时对比，不承诺提速百分比；读取文件字节和 PDF 初始解析仍在首屏之前。尺寸补齐时滚动条长度可能调整；仍会为所有页创建占位元素，超多页 DOM 优化尚未实施。实体打印机未出纸测试。
